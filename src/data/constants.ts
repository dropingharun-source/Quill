/* Single-user personal tool — the owner is hard-coded per the handoff. */
export const OWNER_NAME = 'Harun'
export const OWNER_INITIAL = 'H'

/** Baseline half of the momentum score (the other half is today's line %). */
export const MOMENTUM_BASE = 72
